apiVersion: v1
kind: Service
metadata:
  name: ordering-backgroundtasks
  namespace: eshop-background
  labels:
    app.kubernetes.io/name: ordering-backgroundtasks
    app.kubernetes.io/part-of: eshop
    tier: background
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ordering-backgroundtasks
  ports:
  - name: p80
    port: 80
    targetPort: 80
