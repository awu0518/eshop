apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: eshop-apis
  labels:
    app.kubernetes.io/name: catalog-api
    app.kubernetes.io/part-of: eshop
    tier: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: catalog-api
  ports:
  - name: p80
    port: 80
    targetPort: 80
  - name: p81
    port: 81
    targetPort: 81
