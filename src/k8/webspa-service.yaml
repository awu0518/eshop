apiVersion: v1
kind: Service
metadata:
  name: webspa
  namespace: eshop-frontend
  labels:
    app.kubernetes.io/name: webspa
    app.kubernetes.io/part-of: eshop
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: webspa
  ports:
  - name: p80
    port: 80
    targetPort: 80
