apiVersion: apps/v1
kind: Deployment
metadata:
  name: basket-api
  namespace: eshop-apis
  labels:
    app.kubernetes.io/name: basket-api
    app.kubernetes.io/part-of: eshop
    tier: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: basket-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: basket-api
        app.kubernetes.io/part-of: eshop
    spec:
      serviceAccountName: eshop-developer
      containers:
      - name: basket-api
        image: azw7225/basket-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        - containerPort: 81
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: Development
        - name: ASPNETCORE_URLS
          value: http://0.0.0.0:80
        - name: ConnectionString
          value: basketdata.eshop-data.svc.cluster.local
        - name: identityUrl
          value: http://identity-api.eshop-apis.svc.cluster.local
        - name: IdentityUrlExternal
          value: http://docker.internal.localhost:5105
        - name: EventBusConnection
          value: rabbitmq.eshop-data.svc.cluster.local
        - name: AzureServiceBusEnabled
          value: "False"
        - name: UseLoadTest
          value: "False"
        - name: PATH_BASE
          value: /basket-api
        - name: GRPC_PORT
          value: "81"
        - name: PORT
          value: "80"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 400m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /hc
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /hc
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5