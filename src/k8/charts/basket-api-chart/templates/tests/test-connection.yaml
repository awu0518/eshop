apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "basket-api-chart.fullname" . }}-test-connection"
  labels:
    {{- include "basket-api-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "basket-api-chart.fullname" . }}:80']
  restartPolicy: Never
