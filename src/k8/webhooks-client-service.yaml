apiVersion: v1
kind: Service
metadata:
  name: webhooks-client
  namespace: eshop-background
  labels:
    app.kubernetes.io/name: webhooks-client
    app.kubernetes.io/part-of: eshop
    tier: background
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: webhooks-client
  ports:
  - name: p80
    port: 80
    targetPort: 80
