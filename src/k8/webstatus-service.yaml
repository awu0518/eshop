apiVersion: v1
kind: Service
metadata:
  name: webstatus
  namespace: eshop-monitoring
  labels:
    app.kubernetes.io/name: webstatus
    app.kubernetes.io/part-of: eshop
    tier: monitoring
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: webstatus
  ports:
  - name: p80
    port: 80
    targetPort: 80
