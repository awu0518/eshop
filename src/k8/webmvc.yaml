apiVersion: apps/v1
kind: Deployment
metadata:
  name: webmvc
  namespace: eshop-frontend
  labels:
    app.kubernetes.io/name: webmvc
    app.kubernetes.io/part-of: eshop
    tier: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: webmvc
  template:
    metadata:
      labels:
        app.kubernetes.io/name: webmvc
        app.kubernetes.io/part-of: eshop
    spec:
      serviceAccountName: eshop-frontend-deployer
      containers:
      - name: webmvc
        image: azw7225/webmvc:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: Development
        - name: ASPNETCORE_URLS
          value: http://0.0.0.0:80
        - name: PurchaseUrl
          value: http://webshoppingapigw
        - name: IdentityUrl
          value: http://docker.internal.localhost:5105
        - name: SignalrHubUrl
          value: http://docker.internal.localhost:5202
        - name: IdentityUrlHC
          value: http://identity-api/hc
        - name: UseCustomizationData
          value: "True"
        - name: ApplicationInsights__InstrumentationKey
          value: ""
        - name: OrchestratorType
          value: ""
        - name: UseLoadTest
          value: "False"
        envFrom:
          - configMapRef:
              name: external-links
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 300m
            memory: 256Mi
        # Temporarily disabled for autoscaling demo  
        # livenessProbe:
        #   httpGet:
        #     path: /hc
        #     port: 80
        #   initialDelaySeconds: 30
        #   periodSeconds: 10
        # readinessProbe:
        #   httpGet:
        #     path: /hc
        #     port: 80
        #   initialDelaySeconds: 5
        #   periodSeconds: 5